// Hardcoded Speakeasy luxury fallback theme, kept in sync with the dark
// luxury tokens in src/app/globals.css. Used so the UI never depends on the
// theme API (it also supplies the default before a saved theme arrives).
export const DEFAULT_SPEAKEASY_THEME = {
  bg: "#000000",
  surface: "#121212",
  border: "#262626",
  text: "#f5f4f2",
  muted: "#a3a3a3",
  accent: "#d4af37",
  primary: "#2e7d32",
};